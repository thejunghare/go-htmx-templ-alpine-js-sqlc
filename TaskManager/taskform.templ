package main

templ TaskForm() {
	<form hx-post={``} hx-target="#tasks" hx-swap="beforeend">
		<input type="text" name="title" placeholder="New task..." required/>
		<button type="submit">Add Task</button>
	</form>
}
